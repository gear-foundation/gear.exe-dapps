export type Direction = "up" | "down" | "left" | "right";

export type Section = { size: number; direction: Direction };
export type StartPoint = { x: number; y: number };
export type Rows = number[][];
